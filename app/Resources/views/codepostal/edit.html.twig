{% extends 'base.html.twig' %}
{% block pageheader %}Modifier le code postal{% endblock %}
{% block body %}

    {{ form_start(edit_form) }}
    {{ form_widget(edit_form.code, {'attr': {'placeholder': 'Code', 'class' : 'form-control'}}) }}
    {{ form_widget(edit_form.ville, {'attr': {'placeholder': 'Ville', 'class' : 'form-control'}}) }} 
    {{ form_widget(edit_form) }}
       <input type="submit" value="Modifier" class="btn btn-primary"/>
    {{ form_end(edit_form) }}


    <ul>
        <li>
            <a href="{{ path('codepostal_index') }}">Back to the list</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
