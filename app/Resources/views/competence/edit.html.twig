{% extends 'base.html.twig' %}
{% block pageheader %}Modifier la compétence{% endblock %}
{% block body %}
 
    {{ form_start(edit_form) }}
    {{ form_widget(edit_form.nom, {'attr': {'placeholder': 'Nom de la compétence', 'class' : 'form-control'}}) }}
    {{ form_widget(edit_form.description, {'attr': {'placeholder': 'Description', 'class' : 'form-control'}}) }}
        {{ form_widget(edit_form) }}
        <input type="submit" value="Modifier" class="btn btn-primary"/>
    {{ form_end(edit_form) }}

    <ul>
        <li>
            <a href="{{ path('competence_index') }}">Back to the list</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
